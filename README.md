# ToDo
A full-stack ToDo application built with Next.js (App Router) on the frontend and Node.js, Express, MongoDB on the backend. Users can create, view, edit, and delete todos using a clean and responsive UI. Designed with server-side rendering in mind and minimal client-side overhead.
